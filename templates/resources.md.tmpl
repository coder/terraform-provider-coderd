{{- $subcategory := "" -}}
{{- if or (eq .Name "coderd_agents_default_model") (eq .Name "coderd_agents_mcp_server") (eq .Name "coderd_agents_model") (eq .Name "coderd_agents_system_prompt") -}}
{{- $subcategory = "Agents" -}}
{{- else if eq .Name "coderd_ai_provider" -}}
{{- $subcategory = "AI Governance" -}}
{{- else if or (eq .Name "coderd_license") (eq .Name "coderd_oauth2_provider_settings") (eq .Name "coderd_provisioner_key") (eq .Name "coderd_workspace_proxy") -}}
{{- $subcategory = "Deployment" -}}
{{- else if or (eq .Name "coderd_group") (eq .Name "coderd_organization") (eq .Name "coderd_organization_group_sync") (eq .Name "coderd_organization_sync_settings") (eq .Name "coderd_user") -}}
{{- $subcategory = "Identity & Access Management" -}}
{{- else if eq .Name "coderd_template" -}}
{{- $subcategory = "Templates" -}}
{{- end -}}
---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.RenderedProviderName}}"
subcategory: "{{$subcategory}}"
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

{{ if .HasExamples -}}
## Example Usage

{{- range .ExampleFiles }}

{{ tffile . }}
{{- end }}
{{- end }}

{{ .SchemaMarkdown | trimspace }}
{{- if .HasImport }}

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

{{codefile "shell" .ImportFile }}
{{- end }}
